'use client';

import AskSection from '../../../components/features/chat/ask/AskSection';

// This is the default page for the Chat module (/dashboard/chat).
// As per the plan, it renders the "Ask" section.
export default function ChatPage() {
  return (
    <AskSection />
  );
}